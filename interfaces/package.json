{
    "name": "server_mgt-lib",
    "version": "1.0.0",
    "description": "",
    "scripts": {
        "prepare": "npm run finalbuild",
        "finalbuild": "npx tsc --outDir . ",
        "build": "npx tsc --build",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "universum",
    "license": "MIT",
    "dependencies": {
        "typescript": "^4.5.4"
    }
}
